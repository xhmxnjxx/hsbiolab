import {Text, View} from 'react-native';
import styles from './styles';

const DialysisRoutineCard = ({}) => {
  return (
    <View style={styles.container}>
      <Text>DialysisRoutineCard</Text>
    </View>
  );
};

export default DialysisRoutineCard;
