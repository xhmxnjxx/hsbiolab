import {Text, View} from 'react-native';
import styles from './styles';

const MedicationRoutineCard = ({}) => {
  return (
    <View style={styles.container}>
      <Text>MedicationRoutineCard</Text>
    </View>
  );
};

export default MedicationRoutineCard;
